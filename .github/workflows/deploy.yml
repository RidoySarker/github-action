name: Deploy

on:
  push:
    branches:
      - main
jobs:
  main-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        with:
          ref: ${{ github.ref }}
      - name: Deploy Main
        uses: appleboy/ssh-action@master
